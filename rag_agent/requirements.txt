# Compatibility mirror for tools that still consume requirements.txt.
# pyproject.toml is the dependency source of truth; keep this list synchronized.
chromadb>=0.4.22,<2
numpy>=1.24,<3
rank-bm25>=0.2.2,<1
requests>=2.28,<3
jieba>=0.42.1,<1
flask>=3.0,<4

